import * as React from 'react';
import { Icon, MD3Colors } from 'react-native-paper';

const IconoDiseno = () => (
  <Icon
    source="camera"
    color={MD3Colors.error50}
    size={20}
  />
);

export default IconoDiseno;